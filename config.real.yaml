project: "phagematch_v0_1"

manifests:
  phages: "manifests/phages_real.tsv"
  hosts: "manifests/hosts_real.tsv"

directories:
  cache: "cache"
  logs: "results/logs"
  tmp: "results/tmp"
  rankings: "rankings"
  results_viewer: "results"

modules:
  enable_structural_ppi: true
  enable_host_structure: false
  enable_sourmash: true
  enable_safety: true
  test_mode: true

params:
  foldseek_evalue_max: 1e-3
  top_n: 10
  sourmash_k: 21
  sourmash_scaled: 2000

containers:
  colabfold_image: "ghcr.io/sokrypton/colabfold@sha256:REPLACE_WITH_DIGEST"
  foldseek_image: "ghcr.io/STEINEggerlab/foldseek@sha256:REPLACE_WITH_DIGEST"

databases:
  vfdb: "/abs/path/to/vfdb"
  prokka_db: null

versions:
  hhsearch_db: "pdb70_2024_XX"
  uniclust_db: "uniclust30_2020_06"
  foldseek: "9.427df8a"
  sourmash: "4.x"

profile: "test"

structures:
  phage_library_dir: "cache/structures/phages"
  hosts_dir: "cache/structures/hosts"
